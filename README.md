# Apache-update
apache update through jenkins job by running ansible playbook

Nginx receives the request → forwards it to Apache on port 8081 → Apache processes it → response goes back through Nginx → client.
